CREATEDB galvanize_reads

CREATE TABLE books (
  id SERIAL PRIMARY KEY,
  book_title TEXT,
  book_genre TEXT,
  book_description TEXT,
  book_cover_url TEXT,
  book_authors TEXT
);

CREATE TABLE authors (
  id SERIAL PRIMARY KEY,
  first_name TEXT,
  last_name TEXT,
  portrait_url TEXT,
  biography TEXT
);

CREATE TABLE authors_books (
  id SERIAL PRIMARY KEY,
  book_id INTEGER NOT NULL REFERENCES books(id) ON DELETE CASCADE,
  author_id INTEGER NOT NULL REFERENCES authors(id) ON DELETE CASCADE
);


INSERT INTO books (book_title, book_genre, book_description, book_authors) VALUES  ('JS for dummies', 'JS', 'Learn JS you dummy', 'John Smith');
